---
platform: template
sensors:
  apple_tv_source:
    value_template: >-
      {% if states('media_player.apple_tv_bedroom') != 'idle' %}
        {{ state_attr('media_player.apple_tv_bedroom', 'app_name') }}
      {% else %}
        Not Playing
      {% endif %}
