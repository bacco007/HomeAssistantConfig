---
type: vertical-stack
cards:
  - type: entities
    card_mod:
      style:
        fold-entity-row$: |
          ha-icon {
            color: rgb(var(--rgb-disabled-color));
            margin-right: 24px;
          }
        hui-section-row $: |
          .divider { display: none; }
        .: |
          .card-content {
            padding: 0;
          }
    entities:
      - type: custom:fold-entity-row
        head:
          type: custom:mushroom-template-card
          primary: Top Artists (Today)
          secondary: >
            {{ state_attr('sensor.yourspotify_topartists_today', 'data')[0].artist }}
          icon: mdi:spotify
          icon_color: green
          card_mod:
            style: |
              ha-card {
                background: none;
                border: 0;
                box-shadow: none;
              }
        padding: 0
        entities:
          - type: custom:hui-element
            card_type: custom:flex-table-card
            entities:
              include: sensor.yourspotify_topartists_today
            css:
              table+: "border-collapse: collapse; padding: 0px;"
            columns:
              - name: Artist
                data: data
                modify: x.artist
              - name: Plays
                data: data
                modify: x.count
                align: center
              - name: Unique
                data: data
                modify: x.differents
                align: center
              - name: Duration
                data: data
                align: center
                modify: |-
                  {
                    var ms = Number(x.time_listening_ms);
                    const days = Math.floor(ms / (24*60*60*1000));
                    const daysms = ms % (24*60*60*1000);
                    const hours = Math.floor(daysms / (60*60*1000));
                    const hoursms = ms % (60*60*1000);
                    const minutes = Math.floor(hoursms / (60*1000));
                    const minutesms = ms % (60*1000);
                    const sec = Math.floor(minutesms / 1000);
                    days.toString() + "d" + hours.toString().padStart(2, '0') + ":" + minutes.toString().padStart(2, '0')
                  }
            card_mod:
              style: |
                ha-card {
                  overflow: auto;
                  padding-left: 20px;
                  padding-right: 20px;
                  padding-bottom: 20px
                }
                tbody tr:hover {
                  background-color: coral !important
                }
  - type: entities
    card_mod:
      style:
        fold-entity-row$: |
          ha-icon {
            color: rgb(var(--rgb-disabled-color));
            margin-right: 24px;
          }
        hui-section-row $: |
          .divider { display: none; }
        .: |
          .card-content {
            padding: 0;
          }
    entities:
      - type: custom:fold-entity-row
        head:
          type: custom:mushroom-template-card
          primary: Top Artists (This Week)
          secondary: >
            {{ state_attr('sensor.yourspotify_topartists_thisweek', 'data')[0].artist }}
          icon: mdi:spotify
          icon_color: green
          card_mod:
            style: |
              ha-card {
                background: none;
                border: 0;
                box-shadow: none;
              }
        padding: 0
        entities:
          - type: custom:hui-element
            card_type: custom:flex-table-card
            entities:
              include: sensor.yourspotify_topartists_thisweek
            css:
              table+: "border-collapse: collapse; padding: 0px;"
            columns:
              - name: Artist
                data: data
                modify: x.artist
              - name: Plays
                data: data
                modify: x.count
                align: center
              - name: Unique
                data: data
                modify: x.differents
                align: center
              - name: Duration
                data: data
                align: center
                modify: |-
                  {
                    var ms = Number(x.time_listening_ms);
                    const days = Math.floor(ms / (24*60*60*1000));
                    const daysms = ms % (24*60*60*1000);
                    const hours = Math.floor(daysms / (60*60*1000));
                    const hoursms = ms % (60*60*1000);
                    const minutes = Math.floor(hoursms / (60*1000));
                    const minutesms = ms % (60*1000);
                    const sec = Math.floor(minutesms / 1000);
                    days.toString() + "d" + hours.toString().padStart(2, '0') + ":" + minutes.toString().padStart(2, '0')
                  }
            card_mod:
              style: |
                ha-card {
                  overflow: auto;
                  padding-left: 20px;
                  padding-right: 20px;
                  padding-bottom: 20px
                }
                tbody tr:hover {
                  background-color: coral !important
                }
  - type: entities
    card_mod:
      style:
        fold-entity-row$: |
          ha-icon {
            color: rgb(var(--rgb-disabled-color));
            margin-right: 24px;
          }
        hui-section-row $: |
          .divider { display: none; }
        .: |
          .card-content {
            padding: 0;
          }
    entities:
      - type: custom:fold-entity-row
        head:
          type: custom:mushroom-template-card
          primary: Top Artists (This Month)
          secondary: >
            {{ state_attr('sensor.yourspotify_topartists_thismonth', 'data')[0].artist }}
          icon: mdi:spotify
          icon_color: green
          card_mod:
            style: |
              ha-card {
                background: none;
                border: 0;
                box-shadow: none;
              }
        padding: 0
        entities:
          - type: custom:hui-element
            card_type: custom:flex-table-card
            entities:
              include: sensor.yourspotify_topartists_thismonth
            css:
              table+: "border-collapse: collapse; padding: 0px;"
            columns:
              - name: Artist
                data: data
                modify: x.artist
              - name: Plays
                data: data
                modify: x.count
                align: center
              - name: Unique
                data: data
                modify: x.differents
                align: center
              - name: Duration
                data: data
                align: center
                modify: |-
                  {
                    var ms = Number(x.time_listening_ms);
                    const days = Math.floor(ms / (24*60*60*1000));
                    const daysms = ms % (24*60*60*1000);
                    const hours = Math.floor(daysms / (60*60*1000));
                    const hoursms = ms % (60*60*1000);
                    const minutes = Math.floor(hoursms / (60*1000));
                    const minutesms = ms % (60*1000);
                    const sec = Math.floor(minutesms / 1000);
                    days.toString() + "d" + hours.toString().padStart(2, '0') + ":" + minutes.toString().padStart(2, '0')
                  }
            card_mod:
              style: |
                ha-card {
                  overflow: auto;
                  padding-left: 20px;
                  padding-right: 20px;
                  padding-bottom: 20px
                }
                tbody tr:hover {
                  background-color: coral !important
                }
  - type: entities
    card_mod:
      style:
        fold-entity-row$: |
          ha-icon {
            color: rgb(var(--rgb-disabled-color));
            margin-right: 24px;
          }
        hui-section-row $: |
          .divider { display: none; }
        .: |
          .card-content {
            padding: 0;
          }
    entities:
      - type: custom:fold-entity-row
        head:
          type: custom:mushroom-template-card
          primary: Top Artists (This Year)
          secondary: >
            {{ state_attr('sensor.yourspotify_topartists_thisyear', 'data')[0].artist }}
          icon: mdi:spotify
          icon_color: green
          card_mod:
            style: |
              ha-card {
                background: none;
                border: 0;
                box-shadow: none;
              }
        padding: 0
        entities:
          - type: custom:hui-element
            card_type: custom:flex-table-card
            entities:
              include: sensor.yourspotify_topartists_thisyear
            css:
              table+: "border-collapse: collapse; padding: 0px;"
            columns:
              - name: Artist
                data: data
                modify: x.artist
              - name: Plays
                data: data
                modify: x.count
                align: center
              - name: Unique
                data: data
                modify: x.differents
                align: center
              - name: Duration
                data: data
                align: center
                modify: |-
                  {
                    var ms = Number(x.time_listening_ms);
                    const days = Math.floor(ms / (24*60*60*1000));
                    const daysms = ms % (24*60*60*1000);
                    const hours = Math.floor(daysms / (60*60*1000));
                    const hoursms = ms % (60*60*1000);
                    const minutes = Math.floor(hoursms / (60*1000));
                    const minutesms = ms % (60*1000);
                    const sec = Math.floor(minutesms / 1000);
                    days.toString() + "d" + hours.toString().padStart(2, '0') + ":" + minutes.toString().padStart(2, '0')
                  }
            card_mod:
              style: |
                ha-card {
                  overflow: auto;
                  padding-left: 20px;
                  padding-right: 20px;
                  padding-bottom: 20px
                }
                tbody tr:hover {
                  background-color: coral !important
                }
  - type: entities
    card_mod:
      style:
        fold-entity-row$: |
          ha-icon {
            color: rgb(var(--rgb-disabled-color));
            margin-right: 24px;
          }
        hui-section-row $: |
          .divider { display: none; }
        .: |
          .card-content {
            padding: 0;
          }
    entities:
      - type: custom:fold-entity-row
        head:
          type: custom:mushroom-template-card
          primary: Top Artists (All Time)
          secondary: >
            {{ state_attr('sensor.yourspotify_topartists_alltime', 'data')[0].artist }}
          icon: mdi:spotify
          icon_color: green
          card_mod:
            style: |
              ha-card {
                background: none;
                border: 0;
                box-shadow: none;
              }
        padding: 0
        entities:
          - type: custom:hui-element
            card_type: custom:flex-table-card
            entities:
              include: sensor.yourspotify_topartists_alltime
            css:
              table+: "border-collapse: collapse; padding: 0px;"
            columns:
              - name: Artist
                data: data
                modify: x.artist
              - name: Plays
                data: data
                modify: x.count
                align: center
              - name: Unique
                data: data
                modify: x.differents
                align: center
              - name: Duration
                data: data
                align: center
                modify: |-
                  {
                    var ms = Number(x.time_listening_ms);
                    const days = Math.floor(ms / (24*60*60*1000));
                    const daysms = ms % (24*60*60*1000);
                    const hours = Math.floor(daysms / (60*60*1000));
                    const hoursms = ms % (60*60*1000);
                    const minutes = Math.floor(hoursms / (60*1000));
                    const minutesms = ms % (60*1000);
                    const sec = Math.floor(minutesms / 1000);
                    days.toString() + "d" + hours.toString().padStart(2, '0') + ":" + minutes.toString().padStart(2, '0')
                  }
            card_mod:
              style: |
                ha-card {
                  overflow: auto;
                  padding-left: 20px;
                  padding-right: 20px;
                  padding-bottom: 20px
                }
                tbody tr:hover {
                  background-color: coral !important
                }
