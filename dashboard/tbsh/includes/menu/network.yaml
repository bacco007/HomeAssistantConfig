---
# Network
type: custom:mushroom-template-card
primary: Network
icon: mdi:lan
layout: horizontal
tap_action:
  action: navigate
  navigation_path: network
hold_action:
  action: none
double_tap_action:
  action: none
card_mod:
  style: |
    ha-card {
      --ha-card-border-width: 0;
    }
